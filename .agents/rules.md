---
description: rules for the AI agent working on this project
---

## Git
- **NEVER** run `git commit` or `git push` on your own. The user handles all commits and pushes themselves.
- You may stage files with `git add` if explicitly asked, but never commit or push without explicit user permission.
